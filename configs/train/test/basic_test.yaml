# Based on 'default_train.yaml'

wandb:
  project: train_test_1.7B
  # run_id: null  # Set to existing run ID to resume WandB logging (e.g., "abc123xyz")

model:
  base_model_id: Qwen/Qwen3-1.7B

data:
  default_config: train/defaults/revealing_score_modified.yaml

lora:
  default_config: train/defaults/lora.yaml

train:
  default_config: train/defaults/grpo.yaml

reward:
  default_config: train/defaults/reward_funs.yaml

results:
  base_dir: /home/ubuntu/Obfuscation_Generalization/results/train/Oct_6
  name: CoT_Penalization_8B_speed_test


