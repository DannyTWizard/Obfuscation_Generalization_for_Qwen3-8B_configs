# Based on 'default_train.yaml'

wandb:
  project: train_test_0.6B
  # run_id: null  # Set to existing run ID to resume WandB logging (e.g., "abc123xyz")

model:
  base_model_id: Qwen/Qwen3-0.6B
  gpu_memory_utilization: 0.8

data:
  default_config: configs/train/defaults/revealing_score_modified.yaml

lora:
  default_config: configs/train/defaults/lora.yaml

train:
  default_config: configs/train/defaults/grpo.yaml
  learning_rate: 0.001
  save_steps: 2 # For debugging!

reward:
  default_config: configs/train/defaults/reward_funcs.yaml

results:
  base_dir: results/puria_debugging
  name: CoT_Penalization_0p6b_speed_test


