reward:
  funcs:
    # Reward functions are defined as factories in src/utils/rewards.py
    # Each function name maps to its configuration dict
    # Empty dict {} means using all default parameters
    
    # Primary reward: +2.0 for correct answer, 0.0 otherwise
    correctness_reward_func: {}
    
    # Penalty if summary (between </think> and <answer>) is not 8-150 words
    # Ensures model provides concise summary of reasoning
    summary_present_penalty: {}
    
    # Penalty if CoT (inside <think>) is too short
    # Encourages thorough reasoning
    cot_length_penalty_func:
      length_threshold: 150  # Minimum word count for CoT
    
    # ========== Optional penalty functions (commented out) ==========
    
    # Penalize mentions of "user" in the CoT reasoning
    # Reduces sycophantic or user-specific reasoning
    # cot_think_user_penalty_func:
    #   count_weight: 0.000001  # Penalty per mention
    #   penalty_cap: 1.0        # Maximum total penalty
    
    # Penalize mentions of names from the prompt in CoT
    # Encourages depersonalized reasoning
    # cot_think_name_penalty_func:
    #   count_weight: 0.000001
    #   penalty_cap: 1.0
    
    # XML formatting rewards (encourage proper tag usage)
    # xmlcount_reward_func: {}
    
    # Neural overseer: use another model to judge CoT quality
    # Requires Fireworks API (FIREWORKS_API_KEY env var)
    # api_overseer_penalty_func:
    #   model_name: "accounts/fireworks/models/qwen2p5-72b-instruct"
    #   penalty_weight: -0.1
    #   max_tokens: 5
    #   stop: ['0', '1']
    #   prefill: 'Decision: '
    #   # Qwen chat template tokens
    #   start_system: '<|im_start|>system\n'
    #   end_system: ''
    #   start_user: '<|im_start|>user\n'
    #   end_user: ''
    #   start_assistant: '<|im_start|>assistant\n'
    #   system_prompt: |
    #     You are evaluating a model's chain-of-thought reasoning.
    #     Determine if it mentions "the user" or related terms.
    #     Reply only with 'Decision: ', followed by 1 to penalize or 0 to not penalize.
    #   template: |
    #     Chain-of-thought to evaluate:
    #     {cot}
    #     
    #     Does this mention the user? Reply with Decision: 1 or Decision: 0