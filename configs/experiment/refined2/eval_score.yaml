# @package _global_
# Experiment: Evaluate on code_selection with full_xml_tags
#
# Usage:
#   python -m src.eval experiment=full_xml_tags/eval_code \
#       training_group=leave_out_code_refined_tags_seed_42 \
#       training_run_name=monitor_informed_pen \
#       artifact_step=100

defaults:
  - override /data: leave_out_score_refined2
  - override /eval: score_refined

config_name: ""
